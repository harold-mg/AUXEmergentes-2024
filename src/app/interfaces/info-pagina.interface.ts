
export interface InfoPagina {
  titulo?: string;
  email?: string;
  nombre_corto?: string;
  github?: string;
  facebook?: string;
  whatsapp?: string;
  instagram?: string;
  equipo_trabajo?: any[];
}

/* signo ? hace que sea opcional puede que sea llamo o no*/